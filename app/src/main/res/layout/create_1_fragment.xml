<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.claudius.saveme.Create_stuff.create_1"
    android:orientation="vertical">
    <!-- TODO: Update blank fragment layout -->
    <ProgressBar
        style="@style/Widget.AppCompat.ProgressBar.Horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/progressBar"
        android:max="5"
        android:progress="2" />

    <ImageButton
        android:layout_width="100dp"
        android:layout_height="100dp"
        app:srcCompat="@android:drawable/ic_menu_camera"
        android:id="@+id/picbutton"
        android:layout_marginTop="?attr/actionBarSize"
        android:layout_gravity="center_horizontal"
        />

    <TextView
        android:layout_marginTop="?attr/actionBarSize"
        android:textColor="@color/allblack"
        android:text="@string/prename"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/preNameLabel"
        android:textSize="18sp" />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textPersonName"
        android:ems="10"
        android:imeOptions="actionDone"
        android:id="@+id/NameGirlfriendEditText" />

    <TextView
        android:layout_marginTop="?attr/actionBarSize"
        android:textColor="@color/allblack"
        android:text="@string/residence"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/WohnortLabel"
        android:textSize="18sp" />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textPersonName"
        android:ems="10"
        android:imeOptions="actionDone"
        android:id="@+id/WohnortGirlfriendEditText" />

</LinearLayout>

